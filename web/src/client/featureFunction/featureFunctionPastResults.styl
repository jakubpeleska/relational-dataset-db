@import '../common/table'

.FeatureFunctionPastResults
  font-size 14px

  table
    table()
    table-Sortable()
    width 1100px
    margin-left -54px

  tbody tr
    &:hover
      cursor pointer

  &-sql
    max-width 400px
    white-space nowrap
    text-overflow ellipsis
    overflow hidden
